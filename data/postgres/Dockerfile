FROM postgres:13

# Optional: Copy custom config or scripts
# COPY ./internal/db/schema.sql /docker-entrypoint-initdb.d/schema.sql

# Optional: Install extensions or tools
# RUN apt-get update && apt-get install -y postgresql-contrib

# The rest is handled by the base image